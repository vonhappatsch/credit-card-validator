const multDigits = (value) => {
//   let len = value.slice(-1);
//   len = len.map(Number);
//   console.log(len);
//   let mult = len.map(function(curr, index) {
//     if (index % 2 === 0) {
//       curr = 2 * curr;
//       if (curr > 9) {
//         curr = curr - 9;
//       }
//     } else {
//       curr;
//     }
//     let amount = 0;
//     amount += curr;
//     return amount;
//   });
//    console.log(mult);
//    return mult;
// };